{
  "name": "exportcsv",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "engines": {
    "node": ">=10.0"
  },
  "scripts": {
    "build": " tsc -p .",
    "clean": "rm -rf dist && rm -rf *.tsbuildinfo && mkdir dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "npm run build",
    "start": "node dist/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.2",
    "@types/express-fileupload": "^1.1.0",
    "@types/node": "^13.1.1",
    "body-parser": "^1.20.1",
    "busboy": "^0.3.1",
    "csv-parser": "^2.3.2",
    "dotenv": "^8.2.0",
    "express": "^4.18.2",
    "typescript": "^3.7.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
